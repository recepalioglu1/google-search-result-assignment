import io.github.bonigarcia.wdm.WebDriverManager;import org.openqa.selenium.By;import org.openqa.selenium.Keys;import org.openqa.selenium.WebDriver;import org.openqa.selenium.chrome.ChromeDriver;import org.openqa.selenium.support.ui.ExpectedConditions;import org.openqa.selenium.support.ui.WebDriverWait;public class beQualified {    public static void main(String[] args ){        String sKey = null;        int numResult = 3;        if (args.length == 1) {            sKey = args[0];            driver(sKey, numResult);        } else {            System.out.println("No Arguments found");            System.exit(1);        }    }    public static void driver(String sKey, int numResult) {        // 1. set up driver        WebDriverManager.chromedriver().setup();        WebDriver driver = new ChromeDriver();        WebDriverWait wait = new WebDriverWait(driver, 5);        // 2. navigate web browser        driver.get("https://www.google.com/");        // 3. locate search box and send given arguments        driver.findElement(By.name("q")).sendKeys(sKey + Keys.ENTER);        // 4. synchronize web page until title if found        wait.until(ExpectedConditions.titleContains(sKey));        //5. Print located webelement        String resultState = driver.findElement(By.id("result-stats")).getText();        //6.put the located webelement in iteration in order to achieve the first three results        System.out.println("\n==========================================="                + "\n\t GIVEN ARGUMENT = " + sKey                + "\n\t THE RESULT OF SEARCH = " + numResult + "\n");System.out.println(resultState + "\n======================================");        for (int i = 1; i <=4; i++) {            String path = "(//h3/..)[" + i + "]";            String linkText=driver.findElement(By.xpath(path)).getAttribute("href");            if(linkText!=null ){                System.out.println("- " + linkText);                            }        }        driver.quit();    }}